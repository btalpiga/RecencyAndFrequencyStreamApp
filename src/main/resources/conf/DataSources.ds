<data_sources>
    <data_source IP = "10.100.1.17"
                 DATABASE = "datawarehouse"
                 USERNAME = "postgres"
                 PASSWORD = "postgres10@"
                 JDBC_URL = "jdbc:postgresql://10.100.1.17/datawarehouse"
                 CONNECTION_NAME = "RecencyAndFreqStreamApp"
                 DS_NAME = "datawarehouse"
                 MAX_POOL_SIZE = "2"
                 INITIAL_POOL_SIZE = "1"
                 ACQUIRE_INCREMENT = "1" />

</data_sources>